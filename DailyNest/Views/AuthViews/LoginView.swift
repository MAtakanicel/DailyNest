import SwiftUI
struct LoginView: View {
    var body: some View {
        VStack(spacing: 20){
            Image(.icon)
                .resizable()
                .frame(width: 250, height: 250)

            
            
            
            
        } //VStack
        .background(AppColors.background)
    }
}

#Preview {
    LoginView()
}
